#include <stdio.h>
/**
 * main- serves as entry point for the program
 * return: By default returns zero if no errors otherwise returns nonzero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
